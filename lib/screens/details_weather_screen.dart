import 'package:flutter/material.dart';

class DetailsWeatherScreen extends StatelessWidget {
  const DetailsWeatherScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
